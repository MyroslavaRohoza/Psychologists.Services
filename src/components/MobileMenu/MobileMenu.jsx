import { Drawer } from "@mui/material";

const MobileMenu = () => {
  return (
    <div>
      <Drawer
        // anchor={anchor}
        // open={state[anchor]}
        // onClose={toggleDrawer(anchor, false)}
      >
       <div></div>
      </Drawer>
    </div>
  );
}

export default MobileMenu